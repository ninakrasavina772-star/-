# Полный функционал для всех пользователей по ссылке (нейросеть + быстрый режим).
# Первый деплой на Streamlit Cloud может занять 10–20 минут из‑за onnxruntime.
pillow>=10.0
numpy>=1.26,<2.1
onnxruntime>=1.17.0
streamlit>=1.28
